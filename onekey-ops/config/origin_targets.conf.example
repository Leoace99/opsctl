# name|domain|origin_ip|port|path|slow_time|scheme
#
# 字段说明：
# - name: 备注名（会用于生成状态文件名）
# - domain: 域名
# - origin_ip: 源站 IP
# - port: 端口（默认 443）
# - path: 检测路径（默认 /）
# - slow_time: 慢响应阈值秒数（默认 5）
# - scheme: https/http（默认 https）
#
# 示例：
# srv1|example.com|1.2.3.4|443|/healthz|5|https

srv1|example.com|1.2.3.4|443|/healthz|5|https
srv2|example.net|5.6.7.8|443|/healthz|5|https
